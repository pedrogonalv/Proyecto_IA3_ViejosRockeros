# Configuración del sistema RAG
# Copiar este archivo a .env y ajustar los valores según sea necesario

# Modelo de embeddings (opcional - usa valor por defecto si no se especifica)
# EMBEDDING_MODEL=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2

# Nivel de logs (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Configuración de API OpenAI (si se usa)
# OPENAI_API_KEY=tu-api-key-aqui

# Configuración de procesamiento OCR
# Tesseract debe estar instalado en el sistema
# En macOS: brew install tesseract
# En Ubuntu: sudo apt-get install tesseract-ocr tesseract-ocr-spa

# Configuración de memoria y performance
# MAX_WORKERS=4
# BATCH_SIZE=32